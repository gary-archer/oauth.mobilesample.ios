import SwiftUI

/*
 * Custom colours for our app
 */
struct Colors {

    // Our default blue colour
    static let lightBlue = Color(red: 51 / 255, green: 153 / 255, blue: 255 / 255)

    // The green colour for highlighted values
    static let paleGreen = Color(red: 0 / 255, green: 128 / 255, blue: 0 / 255)
}
