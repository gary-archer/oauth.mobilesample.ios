1. Get the app working reliably - currently the return to app does not work
   Error testing and tracing for API and OAuth requests
   
2. API view models, to simplify use of callbacks
   Perhaps the task should go in the UI class?
   Compare with Kotlin and React

3. Update backlog with any equivalent Kotlin tasks
   README updates
   Blog updates
